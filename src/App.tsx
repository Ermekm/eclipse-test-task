import { type ReactElement } from "react";
import Counter from "./components/Counter";

import "./index.scss";

const App = (): ReactElement => {
  return (
    <div className="app">
      asdf
      <Counter />
    </div>
  );
};

export default App;
